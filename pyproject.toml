[project]
name = "fs-memory-lab"
version = "0.1.0"
description = "A small filesystem-memory harness inspired by Zhou et al."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
fs-memory = "fs_memory_lab.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
